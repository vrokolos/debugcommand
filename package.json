{
    "name": "debugcommand",
    "displayName": "DebugCommand",
    "description": "Adds a vscode command to launch a configuration without selecting it first",
    "version": "0.2.0",
    "publisher": "iosifak",
    "engines": {
        "vscode": "^1.27.0"
    },
    "categories": [
        "Other"
    ],
    "activationEvents": [
        "onCommand:debugcommand.launch"
    ],
    "main": "./out/extension",
    "contributes": {
        "commands": [
            {
                "command": "debugcommand.launch",
                "title": "Launches the given launch.json configuration"
            }
        ]
    },
    "scripts": {
        "vscode:prepublish": "npm run compile",
        "compile": "tsc -p ./",
        "watch": "tsc -watch -p ./",
        "postinstall": "node ./node_modules/vscode/bin/install",
        "test": "npm run compile && node ./node_modules/vscode/bin/test"
    },
    "devDependencies": {
        "typescript": "^2.6.1",
        "vscode": "^1.1.21",
        "tslint": "^5.8.0",
        "@types/node": "^8.10.25",
        "@types/mocha": "^2.2.42"
    }
}
